// Defines the contract for vehicles with two abstract methods: engine() and breaks().
// Any class implementing this interface must provide implementations for these methods.
interface Vehicle {
    void engine();
    void breaks();
}
